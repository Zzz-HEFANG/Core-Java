Object: 所有类的超类
在JAVA中 每一个类都是由Object类扩展而来。
Java 语言规范要求 equals方法具有以下特性： 自反性，对称性，传递性，一致性，对于任意非空引用x, x.equals(null)应该返回false

数组列表的容量 和 大小的 区别： 如果为数组分配100个元素的存储空间 数组就有100 个位置可以用， 而容量为100个元素的数组列表只是有保存100 个元素的潜力。
但是在最初，甚至完成初始化构造之后，数组列表根本不含有任何元素。
new ArrayList<>(100) //capacity is 100
new Employee[100] //size is 100
一旦确定不需要改变数组的大小，就可以调用trimToSize 方法
